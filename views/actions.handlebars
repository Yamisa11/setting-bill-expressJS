<div class="row">
    <h1>Actions</h1>

<table>
    <r>
        <th>Type</th>
        <th>Price</th>
        <th>timestamp</th>

    </tr>
        {{#each actions}}
        <tr>
            <td>{{type}}</td>
            <td>{{cost}}</td>
            <td>{{timestamp}}</td>
        </tr>
        {{/each}}
</table>
</div>